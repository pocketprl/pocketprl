<?xml version="1.0" encoding="utf-8"?>
<!-- Only the CSV export directory is ever shared; nothing under files/ (where the vault lives). -->
<paths>
    <cache-path name="exports" path="exports/" />
</paths>
